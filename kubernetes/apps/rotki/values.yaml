# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/common-3.7.3/charts/library/common/values.schema.json
---
strategy: RollingUpdate

controllers:
  rotki:
    # pod:
    #   securityContext:
    #     runAsUser: 65534
    #     runAsGroup: 100
    #     fsGroup: 65534
    #     fsGroupChangePolicy: "OnRootMismatch"

    containers:
      app:
        image:
          repository: rotki/rotki
          tag: v1.44.0@sha256:14d3e61ed1600f94a7c921ba2b4ab58b7f47a66819418c6150550105cecd0254
        env:
          TZ: Asia/Jerusalem
          ROTKI_ACCEPT_DOCKER_RISK: "1"
        probes:
          liveness: &probes
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /api/1/ping
                port: &port 80
              initialDelaySeconds: 0
              periodSeconds: 10
              timeoutSeconds: 1
              failureThreshold: 3
          readiness: *probes
        resources:
          requests:
            cpu: 10m
            memory: 256Mi
          limits:
            memory: 512Mi
        # securityContext:
        #   allowPrivilegeEscalation: false
        #   readOnlyRootFilesystem: true
        #   capabilities:
        #     drop:
        #       - ALL

service:
  app:
    controller: rotki
    ports:
      http:
        port: *port

route:
  app:
    enabled: true
    kind: HTTPRoute
    hostnames:
      - rotki.lan.peterson.com.ar
    parentRefs:
      - kind: Gateway
        name: internal-gateway
        namespace: network
        sectionName: https
    rules:
      - backendRefs:
          - name: rotki
            port: *port
        matches:
          - path:
              type: PathPrefix
              value: /

persistence:
  config:
    storageClass: zfs-generic-iscsi-csi-xfs
    accessMode: ReadWriteOnce
    size: 700Mi
    globalMounts:
      - path: /data
  logs:
    type: emptyDir
    globalMounts:
      - path: /logs
